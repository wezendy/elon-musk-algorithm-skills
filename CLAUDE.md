# CLAUDE.md

This file delegates to the canonical, vendor-neutral `AGENTS.md` to avoid duplication and drift across AI coding agents.

@AGENTS.md

---

**Note:** Claude Code's `@` import directive automatically loads the referenced file. If you are reading this through a tool that does not support `@import`, open `AGENTS.md` directly. All Claude-specific behavior for this project lives in `AGENTS.md` and the skill files under `skills/`.
